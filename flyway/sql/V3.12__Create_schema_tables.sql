CREATE TABLE IF NOT EXISTS fdw0.book (
    id integer,
    name varchar
);

CREATE TABLE IF NOT EXISTS fdw1.book (
    id integer,
    name varchar
);

CREATE TABLE IF NOT EXISTS fdw2.book (
    id integer,
    name varchar
);

